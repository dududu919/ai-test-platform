# Platform Web

Local control panel for the AI test MVP.

## Start

```bash
npm start
```

Open:

```text
http://127.0.0.1:3200
```

## What it does

- shows whether the demo app is reachable
- triggers `npm run pipeline` in `ai-test-mvp`
- embeds the latest HTML report
- links to the demo app and full report page
